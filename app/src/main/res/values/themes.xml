<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.GeoPal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GeoPal" parent="Base.Theme.GeoPal" />

    <style name="Theme.GeoPal.Fullscreen" parent="Theme.GeoPal">
        <item name="android:actionBarStyle">@style/Widget.Theme.GeoPal.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>
</resources>